"""    app_constants.py        A class containg only read-only application level constants     in the form of class properties. Instances of this class need not be made.        Contains the resource manager references.        Also contains some useful global variables and functions at the end."""import sysimport osimport stringimport resourceTEXTURES = resource.textureManager()class values:    _window_width = 640     # in pixels    _window_height = 480        _frames_per_second = 60        _window_caption = "2D Reflection Demo"        _max_reflection_depth = 5        debug_flag_1 = False        print_texture_loading = True""" global variables """cwd = os.path.dirname(os.path.realpath( __file__ ))""" global functions """def makeRelativePath( path ):    final_path_ = cwd    res_path_ = path.split("/")        for dir in res_path_:        final_path_ = os.path.join(final_path_, dir)        return final_path_