"""    button.py        gui button classes     - uses a gui.label to render button text        'button' normal button class, can tell if it's either pressed or not pressed    * more buttons go here *"""import sysimport osimport mathsys.path.append("C:/Python27/proj/BetterConsoleFunctions/V3")import bcfout = bcfimport pygamefrom pygame.locals import *import labelclass button:        def __init__ ( self ):        """ creates all the button's variables """        pass        def calculate ( self ):        """        (re)renders the button text        should be called if the button text, text size or text colour has changed        """        pass        def update ( self ):        """ does button logic, should be called every frame """        pass        def draw ( self , wsurface ):        """ draws the button """        pass        def pressed ( self ):        """ returns true if the button is pressed, false otherwise """        pass